name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  rust-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            curl \
            file \
            libayatana-appindicator3-dev \
            libssl-dev \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            librsvg2-dev \
            wget

      - name: Normalize formatting
        run: cargo fmt --all

      - name: Compile check
        run: cargo check --all-targets

      - name: Test
        run: cargo test --all-targets

  app-build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Tauri CLI
        run: cargo install tauri-cli --locked --version "^2"

      - name: Generate bundle icons
        run: cargo tauri icon icons/icon.png

      - name: Build bundled macOS app
        run: cargo tauri build --bundles app

      - name: Archive bundled macOS app
        run: |
          ditto -c -k --keepParent \
            "target/release/bundle/macos/MediaVault.app" \
            "target/release/bundle/macos/MediaVault-macos.zip"

      - name: Upload bundled macOS archive
        uses: actions/upload-artifact@v4
        with:
          name: mediavault-macos-app
          path: target/release/bundle/macos/MediaVault-macos.zip
